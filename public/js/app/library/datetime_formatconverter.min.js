const convDateJS = () => { let newDate = new Date(); let months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]; let dateNow = `${newDate.getDate()} ${months[newDate.getMonth()]} ${newDate.getFullYear()}`; return dateNow }
const momentDateNow = () => { moment.locale('id'); return moment().format('DD MMMM YYYY') }